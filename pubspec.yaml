name: tob
description: 赶车网tob应用程序app

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.9+10

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2


  #  屏幕适配
  flutter_screenutil: ^4.0.4+1
  #  路由
  fluro: ^2.0.3
  #   微信
  fluwx: ^3.5.1
  #验证码
  pin_input_text_field: ^4.0.0
  #获取程序应用信息
  package_info: ^2.0.0
  #Flutter插件，用于获取主机平台文件系统上的常用位置，例如temp和app数据目录。
  path_provider: ^2.0.1
  #纯扑吐司库支持自定义样式/小工具。易于使用。您可以使用此库来实现与Android Toast相同的效果
  oktoast: ^3.0.0
  #Dart的功能强大的Http客户端，它支持拦截器，FormData，请求取消，文件下载，超时等。
  dio: ^4.0.0
  #Flutter shared_preferences插件util，支持获取默认值，保存对象，对象列表。
  sp_util: ^2.0.3
  #Flutter Widget，可轻松实现BLoC（业务逻辑组件）设计模式。构建用于与bloc状态管理包一起使用。
  flutter_bloc: ^7.0.0
  #提供给颤动滚动组件的小部件可下拉刷新并拉高负载。
  pull_to_refresh: ^2.0.0
  #clean适用于Flutter的简洁轻巧的加载/吐司小部件，易于使用，无需上下文，支持iOS，Android和Web
  flutter_easyloading: ^3.0.0
  #Flutter插件，用于从Android和iOS图像库中选择图像，并使用相机拍摄新照片。
  image_picker: ^0.7.4
#  #使用本机 (Objective-C/Kotlin) 以更快的速度压缩图像。支持安卓/iOS。
#  flutter_image_compress: ^1.1.0
  #Flutter插件，使用本机iOS / Android调整大小，压缩或裁剪
  flutter_native_image: ^0.0.6
  #Photo View提供了一个手势敏感的可缩放小部件。Photo View主要用于显示交互式图像和其他内容，例如SVG。
  photo_view: ^0.11.1
  #官方扩展图像，支持占位符（加载）/失败状态，缓存网络，缩放/平移，照片视图，滑出页面，编辑器（裁剪，旋转，翻转），绘画等。
  extended_image: ^4.0.1
  #IGUANG正式支持JPush Flutter插件（Android和iOS）。极光推送官方支持的Flutter插件（Android和iOS）（https://www.jiguang.cn）。
  jpush_flutter: ^2.0.1
  #用于创建徽章的颤振包。徽章可用于任何小部件的附加标记，例如，显示购物车中的许多物品。
  badges: ^1.2.0
  #Flutter插件，可在Android和iOS上提供WebView小部件。
  webview_flutter: ^2.0.4
  #Flutter插件，用于启动URL。支持Web，电话，SMS和电子邮件方案。
  url_launcher: ^6.0.3
  #Flutter小部件，将静态HTML和CSS呈现为Flutter小部件。
  flutter_html: ^2.0.0-nullsafety.0
  #Flutter的权限插件。该插件提供了跨平台（iOS，Android）API来请求和检查权限。
  permission_handler: ^7.1.0
  #Flutter 最好的 swiper(carousel)，具有多种布局，无限循环。兼容安卓和iOS
  flutter_swiper: ^1.1.6
  #Flutter PDF Viewer 库用于无缝高效地显示 PDF 文档
  syncfusion_flutter_pdfviewer: ^19.2.47-beta
  #极光官方支持JMLink Flutter插件（Android & iOS）。 https://docs.jiguang.cn
  #jmlink_flutter_plugin: 2.1.2
  #Flutter 选择器库，包括日期时间选择器、单项选择器（性别、职业、教育、星座）、城市地址选择器等
  flutter_pickers: ^2.1.1
  #一个 Flutter 插件，允许您添加内联 web 视图、使用无头 web 视图以及打开应用程序内浏览器窗口。
  flutter_inappwebview: ^5.3.2
dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

    # To add assets to your application, add an assets section, like this:
    # assets:
    #   - images/a_dot_burr.jpeg
    #   - images/a_dot_ham.jpeg

    # An image asset can refer to one or more resolution-specific "variants", see
    # https://flutter.dev/assets-and-images/#resolution-aware.

    # For details regarding adding assets from package dependencies, see
    # https://flutter.dev/assets-and-images/#from-packages

    # To add custom fonts to your application, add a fonts section here,
    # in this "flutter" section. Each entry in this list should have a
    # "family" key with the font family name, and a "fonts" key with a
    # list giving the asset and other descriptors for the font. For
    # example:
    # fonts:
    #   - family: Schyler
    #     fonts:
    #       - asset: fonts/Schyler-Regular.ttf
    #       - asset: fonts/Schyler-Italic.ttf
    #         style: italic
    #   - family: Trajan Pro
    #     fonts:
    #       - asset: fonts/TrajanPro.ttf
    #       - asset: fonts/TrajanPro_Bold.ttf
    #         weight: 700
    #
    # For details regarding fonts from package dependencies,
    # see https://flutter.dev/custom-fonts/#from-packages
  assets:
    - assets/
    - assets/2.0x/
    - assets/3.0x/
